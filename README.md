# adenin-cloud-connector

Repo contains current output of following generator command:

```bash
yo @adenin/cloud-connector adenin-cloud-connector cardservice:card emptyservice:empty sqlservice:sql
```
